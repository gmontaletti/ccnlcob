% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/giornate.R
\name{compute_giornate}
\alias{compute_giornate}
\title{Calcola le giornate di contratto nella finestra di analisi}
\usage{
compute_giornate(dt, window = NULL)
}
\arguments{
\item{dt}{Un \code{data.table} con le colonne \code{inizio} e \code{fine} di classe
\code{Date} (\code{IDate} accettata), tipicamente l'output di
\code{\link[=prepare_rapporti]{prepare_rapporti()}}. Viene modificato per riferimento.}

\item{window}{Vettore di due \code{Date} (o stringhe convertibili),
\code{c(inizio, fine)}, ordinate; \code{NULL} (default) non applica alcun taglio
e conta l'intera durata del rapporto.}
}
\value{
\code{dt} con la colonna \code{giornate} (integer) aggiunta per riferimento,
restituito invisibilmente.
}
\description{
Aggiunge (o sovrascrive) per riferimento la colonna \code{giornate}, cioè il
numero di giorni di ciascun rapporto compresi nella finestra di analisi,
estremi inclusi. La misura è additiva e conta ogni rapporto per intero,
senza correggere le sovrapposizioni fra rapporti della stessa persona
(per quello vedi \code{\link[=compute_giornate_effettive]{compute_giornate_effettive()}}).
}
\details{
Per ogni rapporto
\code{giornate = max(0, min(fine, window[2]) - max(inizio, window[1]) + 1)}.
Un rapporto interamente fuori finestra ha \code{giornate = 0}; un rapporto
interamente dentro ha \code{giornate = fine - inizio + 1}; con \code{window = NULL}
vale sempre \code{fine - inizio + 1} (0 se \code{fine < inizio}). Il calcolo avviene
su giorni interi: la colonna è di tipo integer, mai \code{difftime}. Date
mancanti producono \code{giornate = NA}; le sentinelle non vengono trattate
qui ma in \code{\link[=prepare_rapporti]{prepare_rapporti()}}.
}
\examples{
library(data.table)
dt <- prepare_rapporti(cob_esempio)
compute_giornate(dt, window = as.Date(c("2023-01-01", "2023-12-31")))
dt[giornate > 0, sum(giornate), by = ccnl_key][order(-V1)][1:5]

# senza finestra: intera durata del rapporto
compute_giornate(dt)
dt[, summary(giornate)]
}
\seealso{
Other giornate: 
\code{\link{compute_giornate_effettive}()}
}
\concept{giornate}
