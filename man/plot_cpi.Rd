% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_cpi}
\alias{plot_cpi}
\title{Mappa di calore CCNL per CPI}
\usage{
plot_cpi(
  x,
  keys = NULL,
  value = c("lq", "quota_colonna", "quota_riga"),
  labels = NULL,
  cpi_labels = TRUE
)
}
\arguments{
\item{x}{Una tabella di \code{\link[=ccnl_by_cpi]{ccnl_by_cpi()}}, oppure un oggetto
\code{ccnlcob_result} prodotto da \code{\link[=analyze_ccnl]{analyze_ccnl()}} (viene usato \code{x$cpi}).}

\item{keys}{Vettore character delle chiavi \code{ccnl_key} da mostrare come
righe; \code{NULL} (default) usa \code{x$keys} per un \code{ccnlcob_result},
altrimenti i primi 15 CCNL per totale della misura.}

\item{value}{Colonna da colorare: \code{"lq"} (default), \code{"quota_colonna"} o
\code{"quota_riga"}.}

\item{labels}{\code{data.table} opzionale con le colonne \code{ccnl_key} e
\code{ccnl_titolo} per etichette di riga leggibili; \code{NULL} usa \code{ccnl_key}.}

\item{cpi_labels}{Se \code{TRUE} (default) le colonne sono etichettate con
\code{cpi_name}; se \code{FALSE} con \code{cpi_code}.}
}
\value{
Un oggetto \code{ggplot}.
}
\description{
Griglia CCNL (righe) per CPI (colonne) colorata su \code{lq} (quoziente di
localizzazione, scala divergente centrata su 1), \code{quota_colonna} o
\code{quota_riga} (scala sequenziale su un'unica tinta); in ogni caso la
scala è costruita sulla palette Okabe-Ito. Le colonne CPI sono
ordinate per totale decrescente, con \code{FUORI}/\code{ND} sempre in coda; oltre
20 colonne l'asse diventa illeggibile, quindi vengono
mostrate solo le prime (più \code{FUORI}/\code{ND} se presenti). Le celle
grigie sono combinazioni assenti dai dati oppure, per \code{lq}, mascherate
da \code{min_n} in \code{\link[=ccnl_by_cpi]{ccnl_by_cpi()}}; una didascalia lo ricorda.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  res <- analyze_ccnl(
    cob_esempio,
    window = as.Date(c("2022-01-01", "2024-12-31")),
    lookup_cpi = cpi_esempio,
    top_n = 8,
    min_n = 10
  )
  p <- plot_cpi(res, value = "lq")
}
}
\seealso{
\code{\link[=ccnl_by_cpi]{ccnl_by_cpi()}}.

Other grafici: 
\code{\link{palette_ccnlcob}()},
\code{\link{plot_ranking}()},
\code{\link{plot_ranking_periodo}()},
\code{\link{plot_retribuzioni}()},
\code{\link{plot_tipologie}()},
\code{\link{scale_colour_ccnlcob}()},
\code{\link{theme_ccnlcob}()}
}
\concept{grafici}
