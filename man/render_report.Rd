% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{render_report}
\alias{render_report}
\title{Genera il report HTML dei risultati di analyze_ccnl()}
\usage{
render_report(
  dir,
  output_file = NULL,
  titolo = "Analisi dei CCNL sui dati COB",
  labels = NULL,
  top_n = 15,
  note = NULL,
  quiet = TRUE
)
}
\arguments{
\item{dir}{Directory scritta da \code{\link[=write_results]{write_results()}}; devono esistere almeno
\code{meta.rds} e \code{ranking.rds}.}

\item{output_file}{Percorso del file HTML da produrre; \code{NULL} (default)
scrive \code{report_ccnl.html} dentro \code{dir}. La directory di destinazione
viene creata se assente.}

\item{titolo}{Titolo del report.}

\item{labels}{Etichette leggibili dei CCNL: \code{NULL} (default, usa le
chiavi), un \code{data.frame} con le colonne \code{ccnl_key} (o \code{codice_cnel}) e
\code{ccnl_titolo}, oppure il percorso di un file RDS con la stessa
struttura. Le chiavi duplicate vengono ridotte alla prima occorrenza.}

\item{top_n}{Numero massimo di CCNL nelle classifiche e nelle tabelle;
i grafici a linee ne mostrano al più 8 (capienza della palette).}

\item{note}{Testo libero (character di lunghezza 1) mostrato in apertura
come nota; \code{NULL} per ometterlo.}

\item{quiet}{Se \code{TRUE} (default) sopprime l'output di Quarto; altrimenti
la funzione emette anche un \code{message()} con il tempo impiegato.}
}
\value{
Il percorso di \code{output_file}, invisibilmente, con l'attributo
\code{secondi} (tempo di esecuzione).
}
\description{
Esegue il template Quarto parametrico del pacchetto
(\code{inst/quarto/report_ccnl.qmd}) sulla directory prodotta da
\code{\link[=write_results]{write_results()}} e produce un file HTML autonomo (risorse incorporate)
con classifica dei CCNL, andamento per periodo, distribuzione per CPI e
per tipologia contrattuale, retribuzioni mediane, indicatori di qualità
e note metodologiche. Le sezioni i cui dati non sono presenti nella
directory (per esempio \code{cpi} quando il passo è stato saltato) vengono
sostituite da una frase esplicativa.
}
\details{
La funzione richiede il pacchetto R \code{quarto} e la Quarto CLI (individuata
con \code{quarto::quarto_path()}), oltre a \code{ggplot2} e \code{knitr} per i grafici e
le tabelle; sono tutti in \code{Suggests}. Il template viene copiato in una
directory temporanea, perché Quarto scrive l'HTML accanto al file \code{.qmd},
e il risultato viene poi spostato in \code{output_file}. Il template legge le
tabelle con \code{\link[=readRDS]{readRDS()}} o \code{fst::read_fst()} e ripristina gli attributi
\verb{ccnlcob_*} da \code{meta$attributi}.
}
\examples{
# il template usato dal report
system.file("quarto", "report_ccnl.qmd", package = "ccnlcob")

\dontrun{
# richiede la Quarto CLI
res <- analyze_ccnl(cob_esempio, lookup_cpi = cpi_esempio, min_n = 10)
dir_out <- file.path(tempdir(), "ccnl_report")
write_results(res, dir = dir_out, overwrite = TRUE)
html <- render_report(dir_out, titolo = "CCNL: dati di esempio")
browseURL(html)
}
}
\seealso{
\code{\link[=write_results]{write_results()}} per produrre \code{dir}; \code{\link[=plot_ranking]{plot_ranking()}},
\code{\link[=plot_cpi]{plot_cpi()}}, \code{\link[=plot_tipologie]{plot_tipologie()}}, \code{\link[=plot_retribuzioni]{plot_retribuzioni()}} per i grafici
usati dal template.
}
\concept{report}
