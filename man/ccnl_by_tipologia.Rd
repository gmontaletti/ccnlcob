% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tipologie.R
\name{ccnl_by_tipologia}
\alias{ccnl_by_tipologia}
\title{Distribuisce i CCNL per tipologia contrattuale}
\usage{
ccnl_by_tipologia(
  dt,
  measure = "n_rapporti",
  ccnl = NULL,
  level = c("macro", "codice"),
  orario = TRUE,
  periodo = NULL,
  by = NULL,
  min_n = 30L,
  tipologie = ccnlcob::tipologie_contrattuali
)
}
\arguments{
\item{dt}{Un \code{data.table} di rapporti già passato da \code{\link[=prepare_rapporti]{prepare_rapporti()}}
(colonne \code{ccnl_key}, \code{giornate}, \code{avviato}, \code{attivo}, \code{anno},
\code{trimestre}, \code{macro_tipologia}, \code{orario}). Se \code{macro_tipologia} o
\code{orario} mancano vengono ricalcolate su una copia da
\code{cod_tipologia_contrattuale} (con \code{\link[=classify_tipologia]{classify_tipologia()}}) e da \code{prior}.
\code{dt} non viene modificato.}

\item{measure}{Misura da distribuire, una fra \code{"n_rapporti"} (default),
\code{"n_lavoratori"}, \code{"n_datori"}, \code{"giornate"}, \code{"giornate_effettive"},
\code{"stock"}, con le definizioni di \code{\link[=rank_ccnl]{rank_ccnl()}}.}

\item{ccnl}{\code{NULL} (default) per tutte le chiavi, altrimenti vettore
character delle \code{ccnl_key} da conservare in uscita; \code{NA} conserva i non
classificati. Le chiavi assenti da \code{dt} producono un avviso. Il filtro
agisce dopo il calcolo dei totali (vedi Dettagli).}

\item{level}{Livello della tipologia: \code{"macro"} (macro-classe di
\code{tipologie}) o \code{"codice"} (codice MLPS \code{cod_tipologia_contrattuale},
con la descrizione \code{des_tipologia_contrattuale} letta da \code{tipologie}).}

\item{orario}{Se \code{TRUE} (default) la distribuzione è incrociata anche con
l'orario (\code{"FT"}/\code{"PT"}); con \code{FALSE} la dimensione è omessa.}

\item{periodo}{Colonna temporale (\code{"anno"} o \code{"trimestre"}) per una
distribuzione per coorte di avviamento; \code{NULL} per l'intera finestra.}

\item{by}{Vettore character con colonne aggiuntive di raggruppamento
(es. \code{"sesso"}); \code{NULL} per il solo CCNL.}

\item{min_n}{Soglia sul totale della tipologia (per gruppo) sotto la
quale il quoziente di localizzazione è mascherato con \code{NA}.}

\item{tipologie}{Lookup delle tipologie contrattuali con le colonne
\code{cod_tipologia_contrattuale}, \code{macro_tipologia} e, per
\code{level = "codice"}, \code{des_tipologia_contrattuale}; default
\link{tipologie_contrattuali}.}
}
\value{
Un \code{data.table} con una riga per (\code{by}, \code{periodo}, \code{ccnl_key},
\code{tipologia}, eventualmente \code{orario}) e le colonne: quelle di \code{by} e
\code{periodo}, \code{ccnl_key}, \code{classe} (\code{"CCNL"} o \code{"Non classificati"}),
\code{tipologia}, \code{des_tipologia_contrattuale} (solo con
\code{level = "codice"}), \code{orario} (solo con \code{orario = TRUE}), \verb{<measure>},
\code{quota_riga}, \code{quota_colonna}, \code{lq}; ordinato per gruppo, \code{ccnl_key}
(non classificati in coda) e misura decrescente. L'attributo
\code{ccnlcob_crosstab} riporta \code{measure}, \code{dims}, \code{by}, \code{periodo}, \code{min_n},
\code{level} e \code{orario}. L'input non viene modificato.
}
\description{
Calcola la tabella \code{ccnl_key} x tipologia (x orario) con il valore della
misura scelta, la quota di riga (entro CCNL), la quota di colonna (entro
tipologia) e il quoziente di localizzazione, eventualmente per gruppo
(\code{by}) e per coorte di avviamento (\code{periodo}). La tipologia può essere la
macro-classe di \link{tipologie_contrattuali} o il codice MLPS di dettaglio.
}
\details{
Per ogni gruppo \code{g} (combinazione di \code{by} e \code{periodo}), CCNL \code{c} e cella
di dimensione \code{d} (tipologia, eventualmente incrociata con l'orario),
detto \code{v} il valore della misura nella cella e \code{T} il totale del gruppo:
\itemize{
\item \code{quota_riga = v[c,d] / T[c]}: composizione contrattuale del CCNL,
somma a 1 entro ogni (\code{g}, \code{ccnl_key});
\item \code{quota_colonna = v[c,d] / T[d]}: peso del CCNL entro la tipologia,
somma a 1 entro ogni (\code{g}, \code{d});
\item \code{lq = quota_colonna / (T[c] / T)}: quoziente di localizzazione; valori
maggiori di 1 indicano che il CCNL ricorre alla tipologia più della
media del gruppo. Entro ogni CCNL la media di \code{lq} ponderata con
\code{T[d] / T} è pari a 1.
}

I totali \code{T[c]}, \code{T[d]} e \code{T} sono calcolati su tutte le righe di \code{dt},
inclusi i rapporti con \code{ccnl_key} mancante (classe \code{"Non classificati"},
conservata in uscita) e prima dell'eventuale filtro \code{ccnl}: le quote dei
CCNL conservati non cambiano filtrando. Le celle con \code{T[d] < min_n} hanno
\code{lq = NA} ma non vengono eliminate. Se un totale è zero le quote
corrispondenti sono \code{NA}.

I rapporti con tipologia non riconducibile a una macro-classe formano la
tipologia \code{"Non classificata"} (\code{level = "macro"}); i codici MLPS
mancanti formano la tipologia \code{"ND"} (\code{level = "codice"}), con
\code{des_tipologia_contrattuale} \code{NA} per i codici ignoti al lookup.
L'orario mancante forma la classe \code{"ND"}. Nessun rapporto viene scartato.
Per le misure di conteggio distinto (\code{n_lavoratori}, \code{n_datori}) la somma
delle celle di un CCNL può superare il conteggio del CCNL in
\code{\link[=rank_ccnl]{rank_ccnl()}}, perché una persona può comparire in più tipologie.
}
\examples{
dt <- prepare_rapporti(cob_esempio)
tip <- ccnl_by_tipologia(dt, measure = "n_rapporti", level = "macro")
tip[ccnl_key == "A011"]

# Quote per macro-tipologia senza orario, in forma larga
largo <- ccnl_by_tipologia(dt, orario = FALSE)
data.table::dcast(largo, ccnl_key ~ tipologia, value.var = "quota_riga")[1:5]

# Codici MLPS con descrizione, giornate per anno, solo due CCNL
ccnl_by_tipologia(
  dt,
  measure = "giornate", ccnl = c("A011", "H011"),
  level = "codice", orario = FALSE, periodo = "anno"
)[anno == 2024L]
}
\seealso{
\code{\link[=classify_tipologia]{classify_tipologia()}}, \code{\link[=rank_ccnl]{rank_ccnl()}}, \code{\link[=ccnl_by_cpi]{ccnl_by_cpi()}}.

Other tipologie: 
\code{\link{classify_tipologia}()},
\code{\link{filter_perimetro}()}
}
\concept{tipologie}
