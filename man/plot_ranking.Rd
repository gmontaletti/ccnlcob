% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_ranking}
\alias{plot_ranking}
\title{Grafico a barre del ranking dei CCNL}
\usage{
plot_ranking(x, measure = "giornate", top_n = 15, labels = NULL, other = TRUE)
}
\arguments{
\item{x}{Una tabella di \code{\link[=rank_ccnl]{rank_ccnl()}} o di \code{\link[=select_ccnl_rilevanti]{select_ccnl_rilevanti()}}
(\code{return = "table"}), oppure un oggetto \code{ccnlcob_result} prodotto da
\code{\link[=analyze_ccnl]{analyze_ccnl()}} (viene usato \code{x$ranking}). Se la tabella è
raggruppata per \code{by} o \code{periodo} deve contenere un solo gruppo:
filtrare a monte, altrimenti la funzione segnala un errore.}

\item{measure}{Misura da rappresentare; devono essere presenti le
colonne \code{measure} e \code{quota_measure}.}

\item{top_n}{Numero massimo di CCNL classificati da mostrare.}

\item{labels}{\code{data.table} opzionale con le colonne \code{ccnl_key} e
\code{ccnl_titolo}, per etichette leggibili sull'asse (titolo troncato a
circa 45 caratteri, con la chiave fra parentesi); \code{NULL} (default) usa
direttamente \code{ccnl_key}.}

\item{other}{Se \code{TRUE} (default) aggiunge una barra col residuo dei CCNL
classificati esclusi dal \code{top_n}; se \code{FALSE} quei CCNL non compaiono.}
}
\value{
Un oggetto \code{ggplot}.
}
\description{
Barre orizzontali dei primi \code{top_n} CCNL per \code{measure}, ordinate per
valore decrescente, con la quota sul totale stampata come etichetta
diretta alla fine di ogni barra (nessuna legenda necessaria). I CCNL non
classificati e, con \code{other = TRUE}, il residuo dei CCNL esclusi dal
\code{top_n} sono ricalcolati dalla tabella e disegnati come barre distinte,
in grigio neutro anziché nella palette categoriale: la distinzione non
dipende quindi solo dal colore, ma anche dall'etichetta testuale
sull'asse e dalla posizione (sempre in coda al grafico).
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  res <- analyze_ccnl(
    cob_esempio,
    window = as.Date(c("2022-01-01", "2024-12-31")),
    lookup_cpi = cpi_esempio,
    top_n = 8,
    min_n = 10
  )
  p <- plot_ranking(res, measure = "giornate", top_n = 6)
}
}
\seealso{
\code{\link[=rank_ccnl]{rank_ccnl()}}, \code{\link[=select_ccnl_rilevanti]{select_ccnl_rilevanti()}}.

Other grafici: 
\code{\link{palette_ccnlcob}()},
\code{\link{plot_cpi}()},
\code{\link{plot_ranking_periodo}()},
\code{\link{plot_retribuzioni}()},
\code{\link{plot_tipologie}()},
\code{\link{scale_colour_ccnlcob}()},
\code{\link{theme_ccnlcob}()}
}
\concept{grafici}
