% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/territorio.R
\name{ccnl_by_cpi}
\alias{ccnl_by_cpi}
\title{Distribuisce i CCNL per CPI}
\usage{
ccnl_by_cpi(
  dt,
  measure = "giornate",
  ccnl = NULL,
  geo = c("sede_lavoro", "residenza"),
  lookup = NULL,
  periodo = NULL,
  by = NULL,
  min_n = 30L
)
}
\arguments{
\item{dt}{Un \code{data.table} di rapporti già passato da \code{\link[=prepare_rapporti]{prepare_rapporti()}}
(colonne \code{ccnl_key}, \code{giornate}, \code{avviato}, \code{attivo}, \code{anno},
\code{trimestre}). Se contiene già \code{cpi_code} (ed eventualmente \code{cpi_name})
la colonna viene usata così com'è e \code{geo}, \code{lookup} sono ignorati;
altrimenti il CPI viene calcolato con \code{\link[=add_cpi]{add_cpi()}} su una copia delle
colonne necessarie. \code{dt} non viene modificato.}

\item{measure}{Misura da distribuire, una fra \code{"giornate"} (default),
\code{"giornate_effettive"}, \code{"n_rapporti"}, \code{"n_lavoratori"}, \code{"n_datori"},
\code{"stock"}, con le definizioni di \code{\link[=rank_ccnl]{rank_ccnl()}}.}

\item{ccnl}{\code{NULL} (default) per tutte le chiavi, altrimenti vettore
character delle \code{ccnl_key} da conservare in uscita; \code{NA} conserva i non
classificati. Le chiavi assenti da \code{dt} producono un avviso. Il filtro
agisce dopo il calcolo dei totali (vedi Dettagli).}

\item{geo}{Geografia usata da \code{\link[=add_cpi]{add_cpi()}}: \code{"sede_lavoro"} (default) o
\code{"residenza"}. Riportata nell'attributo del risultato.}

\item{lookup}{Lookup comune -> CPI passato ad \code{\link[=add_cpi]{add_cpi()}}; \code{NULL} usa
\code{longworkR}.}

\item{periodo}{Colonna temporale (\code{"anno"} o \code{"trimestre"}) per una
distribuzione per coorte di avviamento; \code{NULL} per l'intera finestra.}

\item{by}{Vettore character con colonne aggiuntive di raggruppamento
(es. \code{"macro_tipologia"}); \code{NULL} per il solo CCNL.}

\item{min_n}{Soglia sul totale del CPI (per gruppo) sotto la quale il
quoziente di localizzazione è mascherato con \code{NA}.}
}
\value{
Un \code{data.table} con una riga per (\code{by}, \code{periodo}, \code{ccnl_key},
\code{cpi_code}) e le colonne: quelle di \code{by} e \code{periodo}, \code{ccnl_key},
\code{classe} (\code{"CCNL"} o \code{"Non classificati"}), \code{cpi_code}, \code{cpi_name},
\verb{<measure>}, \code{quota_riga}, \code{quota_colonna}, \code{lq}; ordinato per gruppo,
\code{ccnl_key} (non classificati in coda) e misura decrescente. L'attributo
\code{ccnlcob_crosstab} riporta \code{measure}, \code{dims}, \code{by}, \code{periodo}, \code{min_n}
e \code{geo}; se \code{cpi_code} era già presente, \code{geo} è quello registrato da
\code{\link[=add_cpi]{add_cpi()}} nell'attributo \code{ccnlcob_cpi} di \code{dt} (\code{NA} se assente).
L'input non viene modificato.
}
\description{
Calcola la tabella \code{ccnl_key} x CPI con il valore della misura scelta, la
quota di riga (entro CCNL), la quota di colonna (entro CPI) e il
quoziente di localizzazione, eventualmente per gruppo (\code{by}) e per coorte
di avviamento (\code{periodo}).
}
\details{
Per ogni gruppo \code{g} (combinazione di \code{by} e \code{periodo}), CCNL \code{c} e CPI
\code{d}, detto \code{v} il valore della misura nella cella e \code{T} il totale del
gruppo:
\itemize{
\item \code{quota_riga = v[c,d] / T[c]}: distribuzione territoriale del CCNL,
somma a 1 entro ogni (\code{g}, \code{ccnl_key});
\item \code{quota_colonna = v[c,d] / T[d]}: composizione contrattuale del CPI,
somma a 1 entro ogni (\code{g}, CPI);
\item \code{lq = quota_colonna / (T[c] / T)}: quoziente di localizzazione; valori
maggiori di 1 indicano concentrazione del CCNL nel CPI rispetto alla
media del gruppo. Entro ogni CCNL la media di \code{lq} ponderata con
\code{T[d] / T} è pari a 1.
}

I totali \code{T[c]}, \code{T[d]} e \code{T} sono calcolati su tutte le righe di \code{dt},
inclusi i rapporti con \code{ccnl_key} mancante (classe \code{"Non classificati"},
conservata in uscita) e prima dell'eventuale filtro \code{ccnl}: le quote dei
CCNL conservati non cambiano filtrando. Le celle dei CPI con
\code{T[d] < min_n} hanno \code{lq = NA} ma non vengono eliminate. Se un totale è
zero le quote corrispondenti sono \code{NA}.

I CPI seguono le convenzioni di \code{\link[=add_cpi]{add_cpi()}}: \code{"FUORI"} per i comuni fuori
Lombardia e \code{"ND"} per i codici mancanti. Per le misure di conteggio
distinto (\code{n_lavoratori}, \code{n_datori}) la somma delle celle di un CCNL può
superare il conteggio del CCNL in \code{\link[=rank_ccnl]{rank_ccnl()}}, perché una persona può
comparire in più CPI.
}
\examples{
dt <- prepare_rapporti(cob_esempio)
cpi <- ccnl_by_cpi(dt, measure = "giornate", lookup = cpi_esempio)
cpi[ccnl_key == "A011"]

# Concentrazioni territoriali più marcate (lq è NA sotto min_n giornate)
cpi[order(-lq)][1:5]

# Rapporti avviati per CPI di residenza, solo due CCNL, per anno
ccnl_by_cpi(
  dt,
  measure = "n_rapporti", ccnl = c("A011", "H011"),
  geo = "residenza", lookup = cpi_esempio, periodo = "anno"
)[anno == 2024L]
}
\seealso{
\code{\link[=add_cpi]{add_cpi()}}, \code{\link[=rank_ccnl]{rank_ccnl()}}, \code{\link[=ccnl_by_tipologia]{ccnl_by_tipologia()}}.

Other territorio: 
\code{\link{add_cpi}()}
}
\concept{territorio}
