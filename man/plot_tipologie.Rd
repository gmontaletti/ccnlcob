% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_tipologie}
\alias{plot_tipologie}
\title{Barre impilate della distribuzione per tipologia contrattuale}
\usage{
plot_tipologie(x, keys = NULL, labels = NULL, orario = FALSE)
}
\arguments{
\item{x}{Una tabella di \code{\link[=ccnl_by_tipologia]{ccnl_by_tipologia()}}, oppure un oggetto
\code{ccnlcob_result} prodotto da \code{\link[=analyze_ccnl]{analyze_ccnl()}} (viene usato
\code{x$tipologie}).}

\item{keys}{Vettore character delle chiavi \code{ccnl_key} da mostrare;
\code{NULL} (default) usa \code{x$keys} per un \code{ccnlcob_result}, altrimenti i
primi 15 CCNL per totale della misura.}

\item{labels}{\code{data.table} opzionale con le colonne \code{ccnl_key} e
\code{ccnl_titolo} per etichette leggibili; \code{NULL} usa \code{ccnl_key}.}

\item{orario}{Se \code{TRUE} sfaccetta il grafico per \code{orario} (richiede la
colonna \code{orario} in \code{x}, prodotta da
\code{ccnl_by_tipologia(orario = TRUE)}); se \code{FALSE} (default) somma le
quote sull'orario.}
}
\value{
Un oggetto \code{ggplot}.
}
\description{
Barre orizzontali impilate di \code{quota_riga} per macro-tipologia
contrattuale, una per CCNL selezionato. Le nove macro-tipologie di
\link{tipologie_contrattuali} superano la capienza sicura di una palette
categoriale (8 colori): la funzione mantiene le 6 più rilevanti fra i
dati mostrati e accorpa le altre in \code{"Altre tipologie"}. Le percentuali
oltre l'8\% sono stampate direttamente nel segmento, con un colore del
testo (bianco o quasi nero) scelto automaticamente per contrasto sullo
sfondo del segmento.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  res <- analyze_ccnl(
    cob_esempio,
    window = as.Date(c("2022-01-01", "2024-12-31")),
    lookup_cpi = cpi_esempio,
    top_n = 8,
    min_n = 10
  )
  p <- plot_tipologie(res)
}
}
\seealso{
\code{\link[=ccnl_by_tipologia]{ccnl_by_tipologia()}}.

Other grafici: 
\code{\link{palette_ccnlcob}()},
\code{\link{plot_cpi}()},
\code{\link{plot_ranking}()},
\code{\link{plot_ranking_periodo}()},
\code{\link{plot_retribuzioni}()},
\code{\link{scale_colour_ccnlcob}()},
\code{\link{theme_ccnlcob}()}
}
\concept{grafici}
