% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare_rapporti}
\alias{prepare_rapporti}
\title{Prepara i rapporti di lavoro per l'analisi}
\usage{
prepare_rapporti(
  dt,
  as_of = NULL,
  window = NULL,
  ccnl_key = c("codice_cnel", "ccnl_warehouse"),
  chiavi_non_classificate = "CPUB",
  perimetro = c("ccnl", "standard", "completo"),
  tipologie = ccnlcob::tipologie_contrattuali
)
}
\arguments{
\item{dt}{Un \code{data.table} con una riga per rapporto di lavoro; vedi
\code{\link[=validate_rapporti]{validate_rapporti()}} per il contratto dati. Le colonne \code{inizio} e
\code{fine} devono ereditare dalla classe \code{Date} (\code{IDate} accettata: la
classe viene conservata in uscita).}

\item{as_of}{Data di riferimento (\code{Date} o stringa convertibile) per lo
stato dei rapporti (\code{attivo}) e per la chiusura dei rapporti aperti;
\code{NULL} (default) usa la data massima non sentinella osservata fra
\code{inizio} e \code{fine} e non successiva alla data odierna, cioè la data di
riferimento dei dati; le date di fine future (fini presunte o valori
errati come \code{2999-12-31}) non concorrono al default e vengono trattate
come rapporti aperti (\code{fine > as_of}).}

\item{window}{Vettore di due \code{Date} (o stringhe convertibili),
\code{c(inizio, fine)}, che delimita l'analisi; \code{NULL} (default) usa
\code{c(min(inizio), as_of)}, dove il minimo è calcolato sugli avviamenti
non sentinella.}

\item{ccnl_key}{Colonna da usare come chiave CCNL di analisi:
\code{"codice_cnel"} (codice CNEL a 4 caratteri prodotto da \code{cnelR}) oppure
\code{"ccnl_warehouse"} (codice warehouse CO). Con il default (entrambe)
viene usata la prima colonna presente in \code{dt}; se una colonna richiesta
esplicitamente è assente la funzione produce un errore.}

\item{chiavi_non_classificate}{Vettore character di valori di \code{ccnl_key}
da trattare come non classificati (\code{NA} in \code{ccnl_key}), oppure \code{NULL}
per non escludere nulla. Default \code{"CPUB"}: codice di comodo del bridge
di \code{cnelR} per il pubblico impiego, non un CCNL misurabile. Vedi
Dettagli.}

\item{perimetro}{Perimetro contrattuale applicato da \code{\link[=filter_perimetro]{filter_perimetro()}}
prima di ogni altro trattamento: \code{"ccnl"} (default) conserva solo le
tipologie di lavoro subordinato alle quali si applica un CCNL;
\code{"standard"} conserva il perimetro "standard" di \code{cnelR};
\code{"completo"} non esclude nulla. Vedi Dettagli.}

\item{tipologie}{Lookup delle tipologie contrattuali con le colonne
\code{cod_tipologia_contrattuale}, \code{macro_tipologia}, \code{perimetro_ccnl} e
\code{esclusa_standard}; default \link{tipologie_contrattuali}.}
}
\value{
Un nuovo \code{data.table} con le colonne di \code{dt} (rinominate come
descritto) e le colonne derivate elencate nei Dettagli, limitato ai
rapporti interni al perimetro che intersecano la finestra; l'attributo
\code{ccnlcob_meta} riporta i parametri e i conteggi. L'input non viene
modificato.
}
\description{
Normalizza un dataset di rapporti di lavoro conforme al contratto dati:
rinomina le colonne (varianti maiuscole della pipeline e \code{ccnl} in
\code{ccnl_warehouse}), applica il perimetro contrattuale, risolve le
sentinelle sulle date, esclude i rapporti esterni alla finestra di
analisi e aggiunge le colonne derivate usate dalle funzioni analitiche.
L'input non viene mai modificato: la funzione lavora su una copia e
restituisce un nuovo \code{data.table}.
}
\details{
\subsection{Normalizzazione dei nomi}{

Le varianti maiuscole prodotte dalla pipeline COB vengono rinominate solo
se la colonna minuscola corrispondente è assente: \code{INIZIO}, \code{FINE},
\code{COD_TIPOLOGIA_CONTRATTUALE}, \code{COMUNE_SEDE_LAVORO}, \code{COMUNE_LAVORATORE},
\code{RETRIBUZIONE}, \code{ORE_SETTIM_MEDIE} (in \code{ore}), \code{ETA_LAV_INIZIO} (in
\code{eta}), \code{SESSO_LAV} (in \code{sesso}). La colonna sorgente \code{ccnl} (codice
warehouse) viene rinominata \code{ccnl_warehouse} se quest'ultima è assente.
Il contratto dati viene verificato dopo la rinomina.
}

\subsection{Conversione di \code{retribuzione} e \code{ore}}{

Nel file \code{rap.fst} della pipeline COB \code{retribuzione} e \code{ore} sono factor
con livelli zero-padded (es. \code{"000023671"}, \code{"05"}). Se presenti come
factor o character, le due colonne vengono convertite in numeric sui
livelli (\code{as.numeric(levels(f))[f]}) senza avvisi; i valori che non
rappresentano un numero diventano \code{NA} e sono conteggiati nei metadati
\code{n_retribuzione_non_numerica} e \code{n_ore_non_numeriche} (0 quando le
colonne sono già numeriche o assenti). I valori numerici non vengono
alterati: la pulizia della retribuzione è compito di
\code{\link[=clean_retribuzione]{clean_retribuzione()}}, quella delle ore di \code{\link[=normalize_fte]{normalize_fte()}}.
}

\subsection{Perimetro contrattuale}{

Subito dopo la verifica del contratto dati la funzione richiama
\code{\link[=filter_perimetro]{filter_perimetro()}} con il \code{perimetro} scelto:
\itemize{
\item \code{"ccnl"} (default) conserva i rapporti con \code{perimetro_ccnl == TRUE}
nel lookup, cioè il lavoro subordinato al quale si applica un CCNL
(codici \code{A.}, \code{F.}, \code{G.01.00}, \code{G.02.00}, \code{H.01.00}, \code{H.03.00}, \code{I.},
\code{N.}); esclude collaborazioni e parasubordinati (\code{B.}), tirocini e
work experience (\code{C.}), lavoro autonomo nello spettacolo (\code{G.03.00}),
lavoro congiunto in agricoltura (\code{H.02.00}), associazione in
partecipazione (\code{L.}), contratti di agenzia (\code{M.}) e i codici ignoti;
\item \code{"standard"} conserva i rapporti con \code{esclusa_standard == FALSE}
(perimetro di \code{cnelR}), codici ignoti inclusi;
\item \code{"completo"} conserva tutte le righe e riproduce il comportamento
delle versioni fino alla 0.2.0, che non applicavano alcun perimetro.
}

Le righe escluse sono conteggiate in \code{n_dropped_perimetro} e dettagliate
per tipologia in \code{esclusi_perimetro}; \code{\link[=filter_perimetro]{filter_perimetro()}} emette un
\code{message()} riassuntivo, sopprimibile. La data di riferimento di default
(\code{as_of = NULL}) è calcolata prima del filtro, su tutti i rapporti, così
da non dipendere dal perimetro; la finestra di default parte invece dal
primo avviamento interno al perimetro.
}

\subsection{Chiavi non classificate}{

Dopo la scelta della colonna chiave, i valori elencati in
\code{chiavi_non_classificate} diventano \code{NA} in \code{ccnl_key} e confluiscono nel
gruppo "Non classificati" di \code{\link[=rank_ccnl]{rank_ccnl()}}; la colonna sorgente
(\code{codice_cnel} o \code{ccnl_warehouse}) conserva il codice originale e il
numero di righe interessate è riportato nel metadato
\code{n_chiavi_non_classificate}. Il default \code{"CPUB"} è il codice di comodo
assegnato dal bridge di \code{cnelR} ai rapporti del pubblico impiego: non
corrisponde a un contratto dell'archivio CNEL e \code{cnelR} lo conta fra i
non classificati. Con \code{chiavi_non_classificate = NULL} il codice resta
una chiave ordinaria.
}

\subsection{Sentinelle sulle date}{

Questa è l'unica funzione del pacchetto che interviene sulle sentinelle,
con le regole di \code{cnelR} 0.6.0:
\itemize{
\item \code{fine} mancante, \verb{<= 1900-01-01} o \verb{> as_of} (incluso \code{9999-12-31})
identifica un rapporto aperto: \code{fine} diventa \code{as_of} e \code{troncata}
vale \code{1};
\item \code{inizio} mancante o \verb{<= 1900-01-01} diventa \code{2008-03-01} (avvio del
sistema delle Comunicazioni Obbligatorie) e \code{troncata_inizio} vale \code{1};
\item dopo le sostituzioni, \code{fine < inizio} viene riportata a \code{inizio}
(rapporto di un giorno) e conteggiata nei metadati.
}

Se \code{dt} contiene già \code{troncata}, il flag esistente viene combinato in OR
con quello calcolato; \code{troncata} e \code{troncata_inizio} sono integer 0/1.
}

\subsection{Finestra di analisi}{

I rapporti con \code{inizio > window[2]} o \code{fine < window[1]} non
intersecano la finestra e vengono rimossi (conteggiati in
\code{n_dropped_window}). Per i rapporti rimanenti \code{giornate} conta i giorni
interni alla finestra, estremi inclusi, ed è quindi sempre \verb{>= 1}.
}

\subsection{Colonne aggiunte}{
\itemize{
\item \code{ccnl_key} (character): chiave di analisi scelta; \code{NA} resta \code{NA} e i
valori in \code{chiavi_non_classificate} diventano \code{NA};
\item \code{perimetro_ccnl} (logical): appartenenza al perimetro CCNL secondo
\code{tipologie}, \code{FALSE} per i codici ignoti; con \code{perimetro = "ccnl"} è
sempre \code{TRUE};
\item \code{troncata}, \code{troncata_inizio} (integer 0/1): flag delle sentinelle;
\item \code{giornate} (integer): giorni-contratto nella finestra, vedi
\code{\link[=compute_giornate]{compute_giornate()}};
\item \code{avviato} (logical): \code{inizio >= window[1]}, cioè avviamento interno
alla finestra;
\item \code{attivo} (logical): rapporto aperto alla data \code{as_of}, con la
definizione di \code{cnelR} (\code{n_attivi}): \code{inizio <= as_of} e \code{fine}
originale, cioè prima della chiusura delle sentinelle, mancante,
\verb{<= 1900-01-01} o \verb{> as_of} (incluso \code{9999-12-31}). Una cessazione
osservata nel giorno \code{as_of} (\code{fine == as_of}) chiude il rapporto, che
non è attivo; un flag \code{troncata} preesistente in \code{dt} (fine non
osservata, già chiusa a monte alla data di stabilizzazione) conta come
rapporto aperto. Su una slice senza \code{troncata} lo \code{stock} di
\code{\link[=rank_ccnl]{rank_ccnl()}} coincide quindi con \code{n_attivi} di \code{cnelR}. Con una
finestra esplicita che termina prima di \code{as_of}, lo stock si riferisce
comunque ad \code{as_of}: per uno stock alla fine della finestra passare
\code{as_of = window[2]};
\item \code{anno} (integer) e \code{trimestre} (character \code{"YYYY-Qn"}): coorte di
avviamento da \code{inizio};
\item \code{macro_tipologia} (character): macro-classe da \code{tipologie}, vedi
\code{\link[=classify_tipologia]{classify_tipologia()}}; \code{NA} per i codici ignoti;
\item \code{orario} (character): \code{"FT"} se \code{prior == 1}, \code{"PT"} se \code{prior == 0},
\code{NA} altrimenti.
}
}

\subsection{Metadati}{

L'attributo \code{ccnlcob_meta} del risultato è una lista con \code{as_of},
\code{window}, \code{ccnl_key} (nome della colonna usata),
\code{chiavi_non_classificate}, \code{perimetro}, \code{n_input} (righe di \code{dt}),
\code{n_dropped_perimetro}, \code{n_tipologia_ignota}, \code{n_chiavi_non_classificate}
(righe con \code{ccnl_key} portata a \code{NA}), \code{n_dropped_window},
\code{n_sentinel_fine}, \code{n_sentinel_inizio}, \code{n_fine_lt_inizio},
\code{n_retribuzione_non_numerica}, \code{n_ore_non_numeriche} e
\code{esclusi_perimetro} (la tabella \code{esclusi} di \code{\link[=filter_perimetro]{filter_perimetro()}}).
}
}
\examples{
library(data.table)
dt <- prepare_rapporti(cob_esempio)
attr(dt, "ccnlcob_meta")[c(
  "as_of", "n_sentinel_fine", "n_fine_lt_inizio", "n_chiavi_non_classificate"
)]
attr(dt, "ccnlcob_meta")$esclusi_perimetro
dt[, .N, by = .(anno, macro_tipologia)][order(anno, -N)][1:5]

# finestra esplicita, chiave warehouse e nessun filtro di perimetro;
# con chiavi_non_classificate = NULL il codice CPUB resta una chiave
dt24 <- prepare_rapporti(
  cob_esempio,
  as_of = as.Date("2024-12-31"),
  window = as.Date(c("2024-01-01", "2024-12-31")),
  ccnl_key = "ccnl_warehouse",
  chiavi_non_classificate = NULL,
  perimetro = "completo"
)
dt24[, .(n = .N, giornate = sum(giornate)), by = ccnl_key][order(-giornate)]
}
\seealso{
Other ingresso: 
\code{\link{read_rapporti}()},
\code{\link{validate_rapporti}()}
}
\concept{ingresso}
