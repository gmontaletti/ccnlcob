% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tipologie.R
\name{classify_tipologia}
\alias{classify_tipologia}
\title{Classifica i codici di tipologia contrattuale in macro-classi}
\usage{
classify_tipologia(codici, tipologie = ccnlcob::tipologie_contrattuali)
}
\arguments{
\item{codici}{Vettore character di codici MLPS (es. \code{"A.01.00"}); i
factor vengono convertiti in character.}

\item{tipologie}{Lookup con le colonne \code{cod_tipologia_contrattuale} e
\code{macro_tipologia}; default \link{tipologie_contrattuali}. Con codici
duplicati nel lookup vale la prima occorrenza.}
}
\value{
Vettore character della stessa lunghezza di \code{codici} con la
macro-classe; \code{NA} per i codici \code{NA} e per quelli non presenti nel
lookup. Errore se \code{tipologie} non contiene le due colonne richieste.
}
\description{
Traduce un vettore di codici MLPS (\code{cod_tipologia_contrattuale}) nella
macro-classe definita dal lookup \link{tipologie_contrattuali}. La traduzione
è vettorizzata (\code{match()}), conserva lunghezza e ordine di \code{codici} e
non modifica i codici in ingresso (nessuna normalizzazione di maiuscole o
spazi).
}
\examples{
classify_tipologia(c("A.01.00", "A.02.00", "C.01.00", "ZZZ", NA))

# lookup alternativo
lookup <- data.frame(
  cod_tipologia_contrattuale = c("X.01", "X.02"),
  macro_tipologia = c("Classe X1", "Classe X2")
)
classify_tipologia(c("X.02", "X.01", "A.01.00"), tipologie = lookup)
}
\seealso{
Other tipologie: 
\code{\link{ccnl_by_tipologia}()},
\code{\link{filter_perimetro}()}
}
\concept{tipologie}
