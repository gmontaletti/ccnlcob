% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perimetro.R
\name{filter_perimetro}
\alias{filter_perimetro}
\title{Filtra i rapporti per perimetro contrattuale}
\usage{
filter_perimetro(
  dt,
  perimetro = c("ccnl", "standard", "completo"),
  tipologie = ccnlcob::tipologie_contrattuali
)
}
\arguments{
\item{dt}{Un \code{data.table} con la colonna \code{cod_tipologia_contrattuale}
(codice MLPS); può essere il dato grezzo o l'uscita di
\code{\link[=prepare_rapporti]{prepare_rapporti()}}. Non viene modificato.}

\item{perimetro}{Perimetro da applicare:
\itemize{
\item \code{"ccnl"} (default): conserva i rapporti con \code{perimetro_ccnl == TRUE}
nel lookup; i codici ignoti vengono esclusi;
\item \code{"standard"}: conserva i rapporti con \code{esclusa_standard == FALSE}
(perimetro di \code{cnelR}); i codici ignoti vengono conservati;
\item \code{"completo"}: conserva tutte le righe.
}}

\item{tipologie}{Lookup delle tipologie contrattuali con le colonne
\code{cod_tipologia_contrattuale}, \code{perimetro_ccnl} e \code{esclusa_standard}
(le colonne \code{des_tipologia_contrattuale} e \code{macro_tipologia}, se
presenti, arricchiscono la tabella degli esclusi); default
\link{tipologie_contrattuali}.}
}
\value{
Un nuovo \code{data.table} con le righe di \code{dt} interne al perimetro,
la colonna \code{perimetro_ccnl} e l'attributo \code{ccnlcob_perimetro}. L'input
non viene modificato.
}
\description{
Restringe un dataset di rapporti di lavoro alle tipologie contrattuali
alle quali si applica un contratto collettivo, oppure al perimetro
"standard" di \code{cnelR}, e riporta nei metadati quante righe sono state
escluse e per quale tipologia. Alcuni avviamenti presenti nelle
Comunicazioni Obbligatorie non sono rapporti di lavoro subordinato
(collaborazioni e parasubordinati, contratti di agenzia, tirocini e work
experience, associazione in partecipazione, lavoro autonomo nello
spettacolo, lavoro congiunto in agricoltura): includerli nelle
classifiche dei CCNL distorce le misure di rilevanza. L'esclusione è
sempre conteggiata e segnalata, mai silenziosa.
}
\details{
\subsection{Regole del perimetro CCNL}{

Il flag \code{perimetro_ccnl} di \link{tipologie_contrattuali} è derivato dal
prefisso del codice MLPS. Appartengono al perimetro tutti i codici \code{A.}
(tempo indeterminato e determinato, apprendistato inclusi il contratto di
formazione lavoro \code{A.03.04} e l'inserimento \code{A.03.07}, lavoro domestico
\verb{A.04.*}, intermittente, somministrazione, ripartito, a domicilio), i
codici \code{F.} (lavoro marittimo), \code{G.01.00} e \code{G.02.00} (spettacolo
subordinato), \code{H.01.00} e \code{H.03.00} (agricoltura subordinata), \code{I.}
(pubblica amministrazione) e \code{N.} (lavoro con piattaforma). Restano
fuori i codici \code{B.} (collaborazioni, lavoro occasionale,
parasubordinati), \code{C.} (tirocini, LSU, borsa lavoro e work experience),
\code{G.03.00} (lavoro autonomo nello spettacolo), \code{H.02.00} (lavoro
congiunto in agricoltura), \code{L.} (associazione in partecipazione) e \code{M.}
(contratto di agenzia). Un codice assente dal lookup è trattato come
fuori perimetro e conteggiato in \code{n_tipologia_ignota}.
}

\subsection{Perimetro standard}{

Il perimetro \code{"standard"} riproduce quello di \code{cnelR}: esclude solo i
codici con \code{esclusa_standard == TRUE} (\code{C.01.00}, \code{B.04.00}, \code{B.03.00},
\code{A.04.00}, \code{A.04.01}). I codici ignoti vengono conservati e conteggiati.
}

\subsection{Colonna e metadati}{

Il risultato porta sempre la colonna logica \code{perimetro_ccnl}, aggiunta
se assente (una colonna già presente viene conservata così com'è; il
filtro usa comunque il lookup). L'attributo \code{ccnlcob_perimetro} è una
lista con \code{perimetro}, \code{n_input}, \code{n_kept}, \code{n_dropped},
\code{n_tipologia_ignota} e \code{esclusi}, un \code{data.table} con le colonne
\code{cod_tipologia_contrattuale}, \code{des_tipologia_contrattuale},
\code{macro_tipologia} e \code{n} (righe escluse per tipologia, in ordine
decrescente di \code{n}; zero righe se nulla è stato escluso). Quando
vengono escluse righe o compaiono codici ignoti la funzione emette un
\code{message()} riassuntivo, sopprimibile con \code{\link[=suppressMessages]{suppressMessages()}}.
}
}
\examples{
library(data.table)
dt <- filter_perimetro(cob_esempio)
nrow(cob_esempio)
nrow(dt)
info <- attr(dt, "ccnlcob_perimetro")
info[c("perimetro", "n_input", "n_kept", "n_dropped", "n_tipologia_ignota")]
info$esclusi

# perimetro standard di cnelR e dataset completo
std <- filter_perimetro(cob_esempio, perimetro = "standard")
attr(std, "ccnlcob_perimetro")$esclusi
tutto <- filter_perimetro(cob_esempio, perimetro = "completo")
tutto[, .N, by = perimetro_ccnl]
}
\seealso{
\code{\link[=prepare_rapporti]{prepare_rapporti()}}, che applica il filtro in ingresso;
\link{tipologie_contrattuali} per le regole.

Other tipologie: 
\code{\link{ccnl_by_tipologia}()},
\code{\link{classify_tipologia}()}
}
\concept{tipologie}
