% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_rapporti}
\alias{validate_rapporti}
\title{Valida il contratto dati dei rapporti di lavoro}
\usage{
validate_rapporti(dt, require = "base")
}
\arguments{
\item{dt}{Un \code{data.table} con una riga per rapporto di lavoro.}

\item{require}{Vettore character con i blocchi di colonne da verificare.
Valori ammessi: \code{"base"} (sempre incluso), \code{"cpi"}, \code{"retribuzione"},
\code{"datore"}; qualsiasi altro valore, anche in un vettore misto, produce
un errore. Vedi Dettagli.}
}
\value{
\code{dt}, restituito invisibilmente, se tutte le verifiche passano.
In caso contrario un errore che elenca le colonne mancanti
(\verb{Colonne mancanti: ...}), l'assenza di una colonna CCNL o le colonne
data con classe non valida.
}
\description{
Verifica che \code{dt} sia un \code{data.table} e che contenga le colonne richieste
dal contratto dati del pacchetto. Il blocco \code{"base"} è sempre verificato;
i blocchi aggiuntivi (\code{"cpi"}, \code{"retribuzione"}, \code{"datore"}) vengono
controllati solo se richiesti tramite \code{require}.
}
\details{
Blocchi e colonne richieste:
\itemize{
\item \code{"base"}: \code{id}, \code{cf}, \code{inizio}, \code{fine}, \code{cod_tipologia_contrattuale},
\code{prior}, più almeno una fra \code{codice_cnel}, \code{ccnl} e \code{ccnl_warehouse}.
Le colonne \code{inizio} e \code{fine} devono ereditare dalla classe \code{Date}
(\code{IDate} è accettata).
\item \code{"cpi"}: \code{comune_sede_lavoro} (codice Belfiore della sede di lavoro).
\item \code{"retribuzione"}: \code{retribuzione} e \code{ore}.
\item \code{"datore"}: \code{datore} (codice fiscale del datore di lavoro).
}

La funzione non modifica \code{dt}. Le sentinelle sulle date (\code{1900-01-01},
\code{9999-12-31}, \code{fine < inizio}) non vengono verificate qui: sono di
competenza di \code{\link[=prepare_rapporti]{prepare_rapporti()}}.
}
\examples{
\dontrun{
library(data.table)
dt <- copy(cob_esempio)
validate_rapporti(dt)
validate_rapporti(dt, require = c("cpi", "retribuzione"))

# errore: manca la colonna comune_sede_lavoro
validate_rapporti(dt[, !"comune_sede_lavoro"], require = "cpi")
}
}
\seealso{
Other ingresso: 
\code{\link{prepare_rapporti}()},
\code{\link{read_rapporti}()}
}
\concept{ingresso}
