% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_retribuzioni}
\alias{plot_retribuzioni}
\title{Andamento della retribuzione mediana per periodo}
\usage{
plot_retribuzioni(
  x,
  keys = NULL,
  labels = NULL,
  reale = FALSE,
  banda = TRUE,
  copertura_min = NULL
)
}
\arguments{
\item{x}{Una tabella di \code{\link[=median_retribuzione]{median_retribuzione()}} o
\code{\link[=deflate_retribuzione]{deflate_retribuzione()}}, oppure un oggetto \code{ccnlcob_result} prodotto
da \code{\link[=analyze_ccnl]{analyze_ccnl()}} (viene usato \code{x$retribuzioni}).}

\item{keys}{Vettore character delle chiavi \code{ccnl_key} da tracciare, al
massimo 8 (limite della palette Okabe-Ito); \code{NULL} (default) usa
\code{x$keys} per un \code{ccnlcob_result}, altrimenti i primi 6 CCNL per somma
di \code{giornate}.}

\item{labels}{\code{data.table} opzionale con le colonne \code{ccnl_key} e
\code{ccnl_titolo} per etichette leggibili; \code{NULL} usa \code{ccnl_key}.}

\item{reale}{Se \code{TRUE} usa le colonne \verb{_reale} prodotte da
\code{\link[=deflate_retribuzione]{deflate_retribuzione()}} (\code{p25_reale}, \code{mediana_reale}, \code{p75_reale});
se \code{FALSE} (default) usa i valori correnti.}

\item{banda}{Se \code{TRUE} (default) disegna la banda p25-p75 come area
semitrasparente intorno alla mediana.}

\item{copertura_min}{Soglia di \code{copertura} sotto la quale un punto è
disegnato vuoto; \code{NULL} (default) disegna tutti i punti pieni.}
}
\value{
Un oggetto \code{ggplot}.
}
\description{
Linee della retribuzione mediana per periodo di avviamento, una per CCNL
selezionato, con banda p25-p75 opzionale. I periodi mascherati (mediana
\code{NA}) sono omessi; i periodi con \code{copertura} sotto \code{copertura_min} sono
disegnati con punto vuoto (contorno colorato, riempimento bianco)
invece che pieno, come codifica ridondante della bassa affidabilità
del dato rispetto al solo colore della serie. L'asse verticale è
formattato in euro con il punto come separatore delle migliaia.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  res <- analyze_ccnl(
    cob_esempio,
    window = as.Date(c("2022-01-01", "2024-12-31")),
    lookup_cpi = cpi_esempio,
    top_n = 8,
    min_n = 10
  )
  p <- plot_retribuzioni(res, copertura_min = 0.5)
}
}
\seealso{
\code{\link[=median_retribuzione]{median_retribuzione()}}, \code{\link[=deflate_retribuzione]{deflate_retribuzione()}}.

Other grafici: 
\code{\link{palette_ccnlcob}()},
\code{\link{plot_cpi}()},
\code{\link{plot_ranking}()},
\code{\link{plot_ranking_periodo}()},
\code{\link{plot_tipologie}()},
\code{\link{scale_colour_ccnlcob}()},
\code{\link{theme_ccnlcob}()}
}
\concept{grafici}
