% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranking.R
\name{rank_ccnl}
\alias{rank_ccnl}
\title{Ordina i CCNL per misure di rilevanza}
\usage{
rank_ccnl(
  dt,
  measures = c("n_lavoratori", "giornate"),
  by = NULL,
  periodo = NULL,
  ccnl_labels = NULL
)
}
\arguments{
\item{dt}{Un \code{data.table} di rapporti già passato da
\code{\link[=prepare_rapporti]{prepare_rapporti()}}, con almeno le colonne \code{ccnl_key} e \code{giornate};
vedi i Dettagli per le colonne richieste da ciascuna misura.}

\item{measures}{Vettore character con le misure da calcolare, fra
\code{"n_rapporti"}, \code{"n_lavoratori"}, \code{"n_datori"}, \code{"giornate"},
\code{"giornate_effettive"}, \code{"stock"}. La prima misura determina
l'ordinamento delle righe in uscita.}

\item{by}{Vettore character con colonne aggiuntive di raggruppamento
(es. \code{"macro_tipologia"}, \code{"sesso"}); \code{NULL} per il solo CCNL.}

\item{periodo}{Colonna temporale (\code{"anno"} o \code{"trimestre"}, prodotte da
\code{\link[=prepare_rapporti]{prepare_rapporti()}}) per un ranking per coorte di avviamento; \code{NULL}
per l'intera finestra. Viene accodata alle colonne di \code{by}.}

\item{ccnl_labels}{\code{data.table} opzionale con la colonna \code{ccnl_key} e
colonne di etichetta (es. \code{ccnl_titolo}, \code{macro_settore_cnel}) da
agganciare al risultato con un left join; le chiavi devono essere
univoche.}
}
\value{
Un \code{data.table} con una riga per (\code{by}, \code{periodo}, \code{ccnl_key}),
ordinato per gruppo e per \code{rank_} della prima misura, con la riga dei
non classificati in coda a ogni gruppo. Colonne: quelle di \code{by} e
\code{periodo}, \code{ccnl_key}, \code{classe} (\code{"CCNL"} o \code{"Non classificati"}), le
eventuali etichette di \code{ccnl_labels} e, per ciascuna misura \code{m}, \code{m},
\code{quota_m}, \code{rank_m} (integer, \code{NA} per i non classificati) e
\code{quota_cum_m}. L'attributo \code{ccnlcob_ranking} conserva \code{measures}, \code{by}
e \code{periodo}. L'input non viene modificato.
}
\description{
Calcola, per ciascun CCNL (ed eventualmente per gruppo e periodo), le
misure di rilevanza richieste, la quota sul totale, il rank e la quota
cumulata lungo il rank. Non viene costruito alcun indice composito: ogni
misura produce il proprio ordinamento e la selezione dei CCNL rilevanti
avviene a valle con \code{\link[=select_ccnl_rilevanti]{select_ccnl_rilevanti()}}.
}
\details{
Le misure sono definite sulle righe di \code{dt} così:
\itemize{
\item \code{n_rapporti}: numero di rapporti con \code{avviato == TRUE} (avviamento
nella finestra di analisi);
\item \code{n_lavoratori}: numero di \code{cf} distinti fra i rapporti avviati;
\item \code{n_datori}: numero di \code{datore} distinti fra i rapporti avviati;
\item \code{giornate}: somma di \code{giornate} (giorni-contratto nella finestra);
\item \code{giornate_effettive}: somma di \code{giornate_effettive} (giorni-persona
allocati pro quota, vedi \code{\link[=compute_giornate_effettive]{compute_giornate_effettive()}});
\item \code{stock}: numero di rapporti con \code{attivo == TRUE} alla data \code{as_of}
fissata in \code{\link[=prepare_rapporti]{prepare_rapporti()}}.
}

Se \code{avviato} o \code{attivo} mancano vengono considerati \code{TRUE} per tutte le
righe, con un messaggio; un valore \code{NA} equivale a \code{FALSE}. Le colonne
\code{datore} e \code{giornate_effettive} sono richieste solo dalle misure che le
usano.

I rapporti con \code{ccnl_key} mancante formano la classe esplicita
\code{"Non classificati"} e non vengono scartati: la quota \code{quota_m} di ogni
riga è calcolata sul totale del gruppo inclusa questa classe, così
che le quote sommino a 1 entro ogni combinazione di \code{by} e \code{periodo}. Il
rank \code{rank_m} è assegnato solo ai CCNL classificati, in ordine
decrescente della misura, con i pari merito che ricevono il rank minimo
comune; la quota cumulata \code{quota_cum_m} segue l'ordine del rank (a parità
di valore, l'ordine alfabetico di \code{ccnl_key}) ed è \code{NA} per la classe non
classificata. Se il totale del gruppo è zero le quote sono \code{NA}.
}
\examples{
# Tabella preparata a mano da cob_esempio (in uso reale: prepare_rapporti())
dt <- data.table::copy(cob_esempio)[
  fine >= inizio & fine <= as.Date("2024-12-31")
]
dt[, `:=`(
  ccnl_key = codice_cnel,
  giornate = as.integer(fine - inizio + 1L),
  avviato = TRUE,
  attivo = fine >= as.Date("2024-12-31"),
  anno = data.table::year(inizio)
)]

ranking <- rank_ccnl(dt, measures = c("n_lavoratori", "giornate"))
head(ranking)

# Ranking per coorte annuale di avviamento
per_anno <- rank_ccnl(dt, measures = "giornate", periodo = "anno")
per_anno[anno == 2024L & rank_giornate <= 3L]

# Etichette agganciate al risultato
etichette <- data.table::data.table(
  ccnl_key = c("A011", "H011"),
  ccnl_titolo = c("Titolo CCNL A011", "Titolo CCNL H011")
)
rank_ccnl(dt, measures = "giornate", ccnl_labels = etichette)[1:3]
}
\seealso{
\code{\link[=select_ccnl_rilevanti]{select_ccnl_rilevanti()}} per la selezione dei CCNL rilevanti.

Other ranking: 
\code{\link{select_ccnl_rilevanti}()}
}
\concept{ranking}
