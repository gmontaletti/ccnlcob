% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/territorio.R
\name{add_cpi}
\alias{add_cpi}
\title{Aggiunge il CPI (Centro per l'Impiego) ai rapporti di lavoro}
\usage{
add_cpi(dt, geo = c("sede_lavoro", "residenza"), lookup = NULL, quiet = TRUE)
}
\arguments{
\item{dt}{Un \code{data.table} di rapporti con la colonna geografica richiesta
da \code{geo}. Viene modificato per riferimento.}

\item{geo}{Geografia di riferimento: \code{"sede_lavoro"} (default, colonna
\code{comune_sede_lavoro}) o \code{"residenza"} (colonna \code{comune_lavoratore}).}

\item{lookup}{\code{data.table} o \code{data.frame} opzionale con le colonne
\code{belfiore} (codice Belfiore del comune, univoco), \code{cpi_code} e,
facoltativa, \code{cpi_name} (se assente viene riempita con \code{cpi_code}).
\code{NULL} (default) usa \code{longworkR}, che deve essere installato e deve
trovare i file di mapping in \code{SHARED_DATA_DIR/maps}. Vedi
\link{cpi_esempio} per un lookup di esempio.}

\item{quiet}{Se \code{TRUE} (default) l'output a console di \code{longworkR} viene
soppresso; ignorato con un \code{lookup} esplicito.}
}
\value{
\code{dt}, restituito invisibilmente, con le colonne character
\code{cpi_code} e \code{cpi_name} aggiunte o sovrascritte per riferimento.
L'attributo \code{ccnlcob_cpi} è una lista con \code{geo} e \code{source}
(\code{"lookup"} oppure \code{"longworkR"}).
}
\description{
Associa a ciascun rapporto il codice e il nome del CPI a partire dal
codice Belfiore della sede di lavoro (\code{comune_sede_lavoro}) o della
residenza del lavoratore (\code{comune_lavoratore}). Il mapping è fornito da un
\code{lookup} esplicito oppure, in sua assenza, dalle utilità geografiche di
\code{longworkR} (\code{longworkR::add_cpi_via_belfiore()}), che il pacchetto non
reimplementa. Le colonne sono aggiunte a \code{dt} per riferimento.
}
\details{
Con \code{lookup = NULL} il mapping viene costruito una sola volta sui codici
Belfiore distinti e non mancanti della colonna sorgente, non sull'intera
tabella, e poi agganciato ai rapporti. Il join è un aggiornamento per
riferimento: il numero e l'ordine delle righe di \code{dt} non cambiano.

Convenzioni sui residui, così che ogni rapporto resti visibile nelle
distribuzioni territoriali:
\itemize{
\item codice Belfiore presente ma assente dal lookup (tipicamente comune
fuori Lombardia): \code{cpi_code = "FUORI"}, \code{cpi_name = "Fuori Lombardia"};
\item codice Belfiore mancante (\code{NA}): \code{cpi_code = "ND"},
\code{cpi_name = "Non disponibile"}.
}

Colonne \code{cpi_code} e \code{cpi_name} già presenti in \code{dt} vengono sovrascritte.
}
\examples{
library(data.table)
dt <- prepare_rapporti(cob_esempio)
add_cpi(dt, geo = "sede_lavoro", lookup = cpi_esempio)
dt[, .N, by = .(cpi_code, cpi_name)][order(-N)]
attr(dt, "ccnlcob_cpi")

# CPI di residenza del lavoratore
add_cpi(dt, geo = "residenza", lookup = cpi_esempio)
dt[, .N, by = cpi_name][order(-N)][1:5]
}
\seealso{
\code{\link[=ccnl_by_cpi]{ccnl_by_cpi()}} per la distribuzione dei CCNL per CPI;
\link{cpi_esempio} per il lookup dei comuni di \link{cob_esempio}.

Other territorio: 
\code{\link{ccnl_by_cpi}()}
}
\concept{territorio}
