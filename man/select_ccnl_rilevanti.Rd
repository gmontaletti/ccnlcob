% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranking.R
\name{select_ccnl_rilevanti}
\alias{select_ccnl_rilevanti}
\title{Seleziona i CCNL rilevanti da un ranking}
\usage{
select_ccnl_rilevanti(
  ranking,
  measure = "giornate",
  top_n = NULL,
  cum_share = 0.8,
  other_label = "Altri CCNL",
  return = c("keys", "table")
)
}
\arguments{
\item{ranking}{Un \code{data.table} prodotto da \code{\link[=rank_ccnl]{rank_ccnl()}}.}

\item{measure}{Misura su cui operare la selezione (es. \code{"giornate"}); nel
ranking devono essere presenti \code{measure}, \verb{quota_<measure>} e
\verb{rank_<measure>}.}

\item{top_n}{Numero massimo di CCNL da selezionare per gruppo; \code{NULL} per
usare solo \code{cum_share}.}

\item{cum_share}{Soglia di quota cumulata in \verb{(0, 1]}; \code{NULL} per usare
solo \code{top_n}. Almeno uno fra \code{top_n} e \code{cum_share} deve essere fornito.}

\item{other_label}{Etichetta della classe residua nella tabella.}

\item{return}{\code{"keys"} per il vettore delle chiavi selezionate, \code{"table"}
per il ranking con la classe residua.}
}
\value{
Con \code{return = "keys"}, vettore character delle chiavi \code{ccnl_key}
selezionate (unione fra i gruppi, senza duplicati, gruppo per gruppo in
ordine di \verb{rank_<measure>} e, a parità, di \code{ccnl_key}). Con
\code{return = "table"}, un \code{data.table} con le colonne del ranking più la
colonna logica \code{selezionato}, ordinato per gruppo con i CCNL selezionati
(in ordine di \verb{rank_<measure>}), la classe residua e i non classificati;
l'attributo \code{ccnlcob_ranking} è conservato e l'attributo
\code{ccnlcob_selezione} riporta \code{measure}, \code{top_n}, \code{cum_share},
\code{other_label} e le chiavi selezionate (\code{keys}). L'input non viene
modificato.
}
\description{
Individua, entro ogni gruppo di un ranking prodotto da \code{\link[=rank_ccnl]{rank_ccnl()}}, i
CCNL che coprono una quota cumulata della misura scelta e/o i primi
\code{top_n}, restituendo le chiavi selezionate oppure il ranking con i CCNL
non selezionati riassunti in una classe residua.
}
\details{
Entro ogni gruppo (\code{by} e \code{periodo} del ranking, letti dall'attributo
\code{ccnlcob_ranking} o, in sua assenza, dalle colonne che precedono
\code{ccnl_key}) i CCNL classificati sono scorsi in ordine di \verb{rank_<measure>}
(a parità di rank, in ordine di \code{ccnl_key}). Un CCNL viene selezionato
finché la quota cumulata dei CCNL che lo precedono è inferiore a
\code{cum_share}: la selezione include quindi il CCNL con cui la quota cumulata
raggiunge o supera la soglia, ed è l'insieme minimo con copertura
\verb{>= cum_share}. Le quote sono quelle del ranking, calcolate sul totale del
gruppo inclusi i non classificati. Se \code{top_n} è fornito la selezione
viene poi troncata ai primi \code{top_n} CCNL; con entrambi i criteri prevale
quindi il più restrittivo. La classe dei non classificati non è mai
selezionata. Entrambi i parametri sono espliciti: il pacchetto non fissa
soglie a priori.

Con \code{return = "table"} i CCNL classificati non selezionati di ogni gruppo
vengono aggregati in una riga con \code{ccnl_key = other_label} e
\code{classe = "Altri CCNL"}: le misure sono sommate, le quote \verb{quota_*}
ricalcolate sul gruppo, \verb{rank_*}, \verb{quota_cum_*} ed etichette poste a \code{NA}.
La riga dei non classificati è conservata. Per le misure di conteggio
distinto (\code{n_lavoratori}, \code{n_datori}) la somma nella classe residua è la
somma dei conteggi per CCNL, non un conteggio distinto sull'insieme.
}
\examples{
dt <- data.table::copy(cob_esempio)[
  fine >= inizio & fine <= as.Date("2024-12-31")
]
dt[, `:=`(
  ccnl_key = codice_cnel,
  giornate = as.integer(fine - inizio + 1L),
  avviato = TRUE
)]
ranking <- rank_ccnl(dt, measures = "giornate")

# Chiavi che coprono almeno l'80\% delle giornate
select_ccnl_rilevanti(ranking, measure = "giornate", cum_share = 0.8)

# Primi 5 CCNL e tabella con la classe residua
select_ccnl_rilevanti(
  ranking,
  measure = "giornate", top_n = 5, cum_share = NULL, return = "table"
)
}
\seealso{
\code{\link[=rank_ccnl]{rank_ccnl()}} per la costruzione del ranking.

Other ranking: 
\code{\link{rank_ccnl}()}
}
\concept{ranking}
