% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_ranking_periodo}
\alias{plot_ranking_periodo}
\title{Andamento del ranking dei CCNL per periodo}
\usage{
plot_ranking_periodo(
  x,
  measure = "giornate",
  keys = NULL,
  labels = NULL,
  quota = TRUE
)
}
\arguments{
\item{x}{Una tabella di \code{\link[=rank_ccnl]{rank_ccnl()}} calcolata con \code{periodo}, oppure un
oggetto \code{ccnlcob_result} prodotto da \code{\link[=analyze_ccnl]{analyze_ccnl()}} (viene usato
\code{x$ranking_periodo}). Deve contenere esattamente una colonna di
periodo (\code{anno} o \code{trimestre}) e non più di una riga per CCNL e
periodo.}

\item{measure}{Misura da rappresentare; devono essere presenti le
colonne \code{measure} e \code{quota_measure}.}

\item{keys}{Vettore character delle chiavi \code{ccnl_key} da tracciare, al
massimo 8 (limite della palette Okabe-Ito); \code{NULL} (default) usa
\code{x$keys} per un \code{ccnlcob_result}, altrimenti i primi 6 CCNL per somma
di \code{measure} sui periodi.}

\item{labels}{\code{data.table} opzionale con le colonne \code{ccnl_key} e
\code{ccnl_titolo} per etichette leggibili; \code{NULL} usa \code{ccnl_key}.}

\item{quota}{Se \code{TRUE} (default) traccia \code{quota_measure} (asse in
percentuale); se \code{FALSE} traccia il valore grezzo di \code{measure}.}
}
\value{
Un oggetto \code{ggplot}.
}
\description{
Linee con punti dell'andamento di \code{measure} (o della sua quota) per
periodo di avviamento, una per CCNL selezionato. Colore, tratteggio e
forma del punto codificano insieme il CCNL (codifica ridondante), con
un'etichetta diretta a fine linea al posto della legenda.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  res <- analyze_ccnl(
    cob_esempio,
    window = as.Date(c("2022-01-01", "2024-12-31")),
    lookup_cpi = cpi_esempio,
    top_n = 8,
    min_n = 10
  )
  p <- plot_ranking_periodo(res, measure = "giornate")
}
}
\seealso{
\code{\link[=rank_ccnl]{rank_ccnl()}}.

Other grafici: 
\code{\link{palette_ccnlcob}()},
\code{\link{plot_cpi}()},
\code{\link{plot_ranking}()},
\code{\link{plot_retribuzioni}()},
\code{\link{plot_tipologie}()},
\code{\link{scale_colour_ccnlcob}()},
\code{\link{theme_ccnlcob}()}
}
\concept{grafici}
