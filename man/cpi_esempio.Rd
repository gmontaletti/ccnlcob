% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{cpi_esempio}
\alias{cpi_esempio}
\title{Lookup comune -> CPI per i comuni di cob_esempio}
\format{
Un \code{data.table} con una riga per comune e le colonne:
\describe{
\item{belfiore}{character. Codice Belfiore (catastale) del comune, chiave
univoca.}
\item{cpi_code}{character. Codice del CPI di competenza (codice
Belfiore del comune sede del CPI seguito dal codice ufficio).}
\item{cpi_name}{character. Denominazione del CPI (es. \verb{CPI MILANO}).}
}
}
\source{
Generato da \code{data-raw/cpi_esempio.R} con
\code{longworkR::add_cpi_via_belfiore()} (utilità geografiche di \code{longworkR}
sui file \code{belfiore_istat_mapping.csv} e \code{comune_cpi_lookup.rds} in
\code{SHARED_DATA_DIR/maps}), 2026-09-09.
}
\usage{
cpi_esempio
}
\description{
Tabella di raccordo fra i codici Belfiore usati in \link{cob_esempio} (sede di
lavoro e residenza) e il CPI (Centro per l'Impiego) di competenza,
risolta con le utilità geografiche di \code{longworkR}. Contiene solo i comuni
lombardi risolti (i dodici capoluoghi); i quattro codici fuori regione
presenti in \link{cob_esempio} (\code{F952}, \code{G535}, \code{H501}, \code{L219}) non compaiono
e con \code{\link[=add_cpi]{add_cpi()}} ricevono il residuo \code{FUORI}. Consente di eseguire gli
esempi di \code{\link[=add_cpi]{add_cpi()}} e \code{\link[=ccnl_by_cpi]{ccnl_by_cpi()}} senza \code{longworkR} e senza i file
di mapping.
}
\examples{
cpi_esempio
}
\seealso{
\code{\link[=add_cpi]{add_cpi()}}, \code{\link[=ccnl_by_cpi]{ccnl_by_cpi()}}, \link{cob_esempio}
}
\keyword{datasets}
